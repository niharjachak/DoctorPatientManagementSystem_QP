import { createContext } from "react";
// AuthContext is a React Context that holds the authentication state and functions for the application,
// allowing components to access the current user's authentication status, role, and other related information.
export const AuthContext = createContext(null);
